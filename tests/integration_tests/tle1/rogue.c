#include <stdio.h>

int main() {
  printf("checkpoint1");
  fflush(stdout);
  long long n, i;
  scanf("%lld", &n);
  for (i = 0; i < n; i++) {
    int a = 1;
  }
  printf("checkpoint2");
  return 0;
}